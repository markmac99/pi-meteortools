name: testing 
on:
    push:
        branches: dev
jobs:
    apitests:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v3
        - run: chmod +x ./tests/*.sh
        - run: ./tests/api_tests.sh
