name: testing 
on:
    push:
        branches: dev
jobs:
    apitests:
        runs-on: thelinux
        steps:
        - uses: actions/checkout@v3
        - run: chmod +x ./tests/*.sh
        - run: ./tests/api_tests.sh
